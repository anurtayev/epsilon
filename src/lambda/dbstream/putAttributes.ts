import { ExtractedMeta } from "./extractMeta";

/**
 * Put all attributes into Attributes table if they do not exist.
 */
export default (attributes: ExtractedMeta[number]["attributes"]): void => {
  console.log(attributes);
};
