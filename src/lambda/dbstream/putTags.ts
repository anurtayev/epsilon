import { ExtractedMeta } from "./extractMeta";

/**
 * Add all tags to the Tags table if they do not exists.
 */
export default (tags: ExtractedMeta[number]["tags"]): void => {
  console.log(tags);
};
