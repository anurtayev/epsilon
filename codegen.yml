overwrite: true
schema: "./src/cloudformation/schema.graphql"
generates:
  ./src/libs/graphqlTypes.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  ./graphql.schema.json:
    plugins:
      - "introspection"
